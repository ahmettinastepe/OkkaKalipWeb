@model Product
<div class="col-md-6">
    <div class="product-box">
        <div class="product-head">
            <img src="~/img/product/@Model.ImageUrl" alt="@Model.Name">
            <div class="product-btn">
                <!--
                @if (User.Identity.IsAuthenticated)
                {
                    <form asp-controller="Cart" asp-action="AddToCart" method="post" class="form-inline">
                        <input type="hidden" name="productId" value="@Model.Id" />
                        <input type="hidden" name="quantity" value="1" />
                        <button class="btn btn-primary" type="submit">Sepete Ekle</button>
                    </form>
                }
                -->
                <a asp-controller="Shop" asp-action="Details" asp-route-id="@Model.Id" class="btn view-detail container-fluid">Ürün Detayı</a>
            </div>
        </div>
        <div class="product-content">
            <div class="product-price"><span class="sale">@Model.Price </span><span class="price">@Model.Price</span></div>
            <h3><a asp-controller="Shop" asp-action="Details" asp-route-id="@Model.Id">@Model.Name </a></h3>
        </div>
    </div>
</div>